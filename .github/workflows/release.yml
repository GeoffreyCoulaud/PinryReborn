name: Release

on:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  # Same quality gate as on PRs. Job name must stay "validate" so it produces
  # the "validate / gate" check. push=true publishes the multi-arch image to
  # GHCR; packages:write lets the reusable build-image job push.
  validate:
    permissions:
      contents: read
      packages: write
    uses: ./.github/workflows/validate.yml
    with:
      push: true
